---
uid: 'textsum'
title: 'TEXTSUM'
pdf: true
---
# TEXTSUM
Выполняет конкатенацию значений ячеек куба X по всем измерениям с заданными правилами. Если указаны дополнительные кубы-признаки Y1, … , Ym, то каждая ячейка куба X попадет в ячейку-«сумму», соответствующую по координатам значениям кубов признаков. 

## Синтаксис

```TEXTSUM (X)``` 

```
TEXTSUM (X, Y1, ..., Ym 
    [ , LIMIT =<Число>]
    [ , DISTINCT]
    [ , VALUE=`формула`]
    [ , ORDER= (`Измерение1`: опция1, опция2,…, опцияN, 
        `Измерение2`: опция1, опция2,…, опцияN, …, 
        `ИзмерениеN`: опция1, опция2,…, опцияN)] )
```

## Аргументы
- X: куб в текстовом формате (Текст/Text)
- Y1, …, Ym: кубы в формате измерений (или их выборок), соответствующих измерениям результирующего мультикуба

Именованные аргументы:

- LIMIT=значение – задает ограничение на результат конкатенации в байтах
- DISTINCT – исключение дубликатов
- VALUE= `формула` – значение преобразуется в соответствии с данной формулой для последующей конкатенации результата
- ORDER=(`Измерение1`:опция1, опция2,…, опцияN, `Измерение2`:опция1, опция2,…, опцияN, …, `ИзмерениеN`: опция1, опция2,…, опцияN) – порядок измерений, в котором происходит конкатенация и опции для этих измерений 

!!! note "Важно" 
    Аргумент ORDER должен либо быть корректно заполнен из всех измерений, либо должен отсутствовать, тогда используется следующий порядок по умолчанию:

    - измерения времени
    - версии
    - выборки кубов
    - справочники в порядке возрастания их идентификаторов.

Опции для измерений:

- ASC – элементы измерения в порядке возрастания
- DESC – элементы измерения в порядке убывания
- SEPARATOR – разделитель между элементами измерения, по умолчанию ", " 

!!! warning ""
    Функционал в доработке: на данный момент рекомендуется указывать одинаковые сепараторы для всех измерений. 

## Возвращаемое значение 

текстовый формат 

## Эквивалент в Excel

ОБЪЕДИНИТЬ/TEXTJOIN 

## Примеры

Рассмотрим мультикуб `Источник`, в строках которого находится нумерованный справочник `Нум 10`, а в колонках кубы `Продукт`, `Регион` – текстового формата, и `Месяц` – формат измерения времени. 

![Мультикуб `Источник`](f-textsum-source-001.png)

Результат работы функции разместим в мультикубе с измерением времени в строках и кубах в колонках. 

Самый простой вариант применения к мультикубу `Источник` функции можно записать формулой: 

```
TEXTSUM('Источник'.'Продукт')
```

В результате все 8 записей из куба Продукты будут объединены в одну ячейку с разделителем по умолчанию «,».  

Чтобы каждая ячейка куба `Источник` попала в ячейку-«сумму», в соответствии со значением месяца, используем формулу: 

```
TEXTSUM('Источник'.'Продукт', 'Источник'.'Месяц')
``` 

![Пример TEXTSUM по месяцам](f-textsum-result-001.png)

Использование именованного аргумента `LIMIT` со значением 10 оставит в результате только часть строки, соответствующую 10 байтам, остальная часть будет отброшена:  

```
TEXTSUM('Источник'.'Продукт', 'Источник'.'Месяц', LIMIT=10)
```

Применение именованного аргумента `DISTINCT` обеспечит уникальное вхождение значений из агрегатного куба:

```
TEXTSUM('Источник'.'Продукт', DISTINCT)
``` 

![Пример TEXTSUM c LIMIT и DISTINCT](f-textsum-result-002.png)

Использование функции [CURRENT_VALUE](current_value.md) в аргументе `VALUE` позволяет вывести текущее значение ячейки и дополнить его. В примере к текущему значению добавлена строка "oooooo": 

```
TEXTSUM('Источник'.'Продукт', LIMIT=16, DISTINCT, VALUE=`CURRENT_VALUE & "oooooo"`)
```

В аргументе VALUE может быть задана любая константа: 

```
TEXTSUM('Источник'.'Продукт', LIMIT=16, DISTINCT, VALUE=`"oooooo"`)
```

![Пример TEXTSUM c CURRENT_VALUE и VALUE](f-textsum-result-003.png)


Чтобы объединение текстовых значений из агрегатного куба происходило в обратном порядке, используйте именованный аргумент `ORDER` с опцией `DESC`. Если указать значение аргумента `SEPARATOR`, объединение будет выполнено с использованием указанных в формуле разделителей: 

```
TEXTSUM('Источник'.'Регион', ORDER=('Нум 10':DESC, SEPARATOR:"**"))
``` 

![Пример TEXTSUM c ORDER](f-textsum-result-004.png)
 