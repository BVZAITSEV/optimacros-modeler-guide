---
uid: 'dimensionsum'
title: 'DIMENSIONSUM'
pdf: true
---
# DIMENSIONSUM
Возвращает в куб суммарное значение по кубу Х по измерению Y в промежутке от элемента Y1 до элемента Y2. 

Если параметры Y1 и Y2 не указаны – в промежутке от первого до последнего элемента измерения Y. 

## Синтаксис

DIMENSIONSUM(X, Y) 

DIMENSIONSUM(X, Y, Y1, Y2) 

## Аргументы 

- X: куб в формате Number
- Y: измерение, используемое в мультикубе или его подмножество (формат List / List Subset, Time / Time Subset, Versions / Versions Subset)
- Y1,Y2: выражения, возвращающие элементы измерения Y 

Могут использоваться относительные значения, например, `0` – текущий элемент, `-1` – предыдущий, `1` - следующий после текущего.

## Возвращаемое значение 

Формат Number 

## Эквивалент в Excel

нет 

### Примеры

Мультикуб, в колонках которого находится справочник `Продукты`, в строках - кубы для расчета:  

| Формула куба| Описание |
| --- | --- |
| DIMENSIONSUM(Продажи, Продукты) | сумма по кубу `Продажи` по справочнику `Продукты` в промежутке от первого элемента до последнего |
| DIMENSIONSUM(Продажи, Продукты, Продукты.Бананы, -1) | сумма по кубу Продажи по справочнику Продукты в промежутке от элемента Бананы до предыдущего |
| DIMENSIONSUM(Продажи, Продукты, FINDBYCODE(Продукты, "BAN003"), -1) | сумма по кубу Продажи по справочнику Продукты в промежутке от элемента, код которого соответствует значению `BAN003` до предыдущего | 
| DIMENSIONSUM(Продажи, Продукты, PARENT(ITEM(Продукты)), 0) | сумма по кубу `Продажи` по справочнику `Продукты` в промежутке от элемента, который является родительским для текущего до текущего |
| DIMENSIONSUM(Продажи, Продукты, 0, PARENT(ITEM(Продукты))) | сумма по кубу `Продажи` по справочнику `Продукты` в промежутке от текущего элемента до элемента, который является родительским для текущего |
| DIMENSIONSUM(Продажи, Продукты, 0, PARENT(Продукты.Морковь)) | сумма по кубу `Продажи` по справочнику `Продукты` в промежутке от текущего элемента до элемента, который является родительским для элемента Морковь |
| DIMENSIONSUM(Продажи, 'выб-ка.Фрукты', Продукты.Яблоки, Продукты.Груши) | сумма по кубу `Продажи` по выборке `Фрукты` справочника `Продукты` в промежутке от элемента `Яблоки` до `Груши`  |
| DIMENSIONSUM(Продажи, 'выб-ка.Фрукты', -1, -1) | сумма по кубу `Продажи` по выборке `Фрукты` справочника `Продукты` предыдущего элемента (относительно текущего) |
| DIMENSIONSUM(Продажи, 'выб-ка.Фрукты', Продукты.Яблоки, 0) | сумма по кубу `Продажи` по выборке `Фрукты` справочника `Продукты` в промежутке от элемента Яблоки до текущего |
| DIMENSIONSUM(Продажи, 'выб-ка.Фрукты', Продукты.Яблоки, -1) | сумма по кубу `Продажи` по выборке `Фрукты` справочника `Продукты` в промежутке от элемента `Яблоки` до предыдущего |
| DIMENSIONSUM(Продажи, 'выб-ка.Овощи', Продукты.Морковь, Продукты.Салат) | сумма по кубу `Продажи` по выборке `Овощи` справочника `Продукты` в промежутке от элемента `Морковь` до `Салат` |
| DIMENSIONSUM(Продажи, 'выб-ка.Овощи', 1, 1) | сумма по кубу `Продажи` по выборке `Овощи` справочника `Продукты` следующего элемента (относительно текущего) |
| DIMENSIONSUM(Продажи, 'выб-ка.Овощи', Продукты.Морковь, 0) | сумма по кубу `Продажи` по выборке `Овощи` справочника `Продукты` в промежутке от элемента `Морковь` до текущего |
| DIMENSIONSUM(Продажи, 'выб-ка.Овощи', s(Продукты, "Морковь"), 0) | сумма по кубу `Продажи` по выборке `Овощи` справочника `Продукты` в промежутке от элемента c названием  "Морковь" до текущего |
| DIMENSIONSUM(Продажи, 'выб-ка.Овощи', Продукты.Морковь, -1) | сумма по кубу `Продажи` по выборке `Овощи` справочника `Продукты` в промежутке от элемента `Морковь` до предыдущего |
 
![Пример функции DIMENSIONSUM](f-dimensionsum-result-001.png)
 