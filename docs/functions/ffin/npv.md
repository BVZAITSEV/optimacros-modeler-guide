---
uid: 'npv'
title: 'NPV'
pdf: true
---
# NPV
Возвращает чистую приведенную стоимость инвестиции, используя ставку дисконтирования, а также последовательность будущих выплат (отрицательные значения) и поступлений (положительные значения). 

NPV определяет, какую сумму прибыли планирует получить инвестор за вычетом всех выплат после того, как окупится первоначальный вклад. 

## Синтаксис

NPV(X, R) 

## Аргументы 

- X: куб определяющий денежный поток в числовом формате (Число/Number) с измерением времени
- R: числовое значение, равное ставке дисконтирования или куб в числовом формате (Число\Number), имеющий измерение времени. 

## Возвращаемое значение 

формат Number 

## Эквивалент в Excel

ЧПС/NPV 

## Особенности NPV

- Если в целевом кубе есть измерение времени, то NPV учитывает периоды начиная от текущего и до последнего в измерении времени. 
- Если в целевом кубе нет измерения времени, а в кубе X есть, то учитываются все периоды в измерении времени. 
- Если ни в одном из кубов нет измерения времени, то функция возвращает значение из X, дисконтированное на один период по ставке из R. 
- В случае невозможности найти решение функция возвращает 0. 
- Решение ищется с точностью до 5 знаков после запятой. 
- Функция дисконтирует первый денежный поток. Если в первой ячейке куба содержится значение первоначальных инвестиций `CF0 (Initial Investment)`, то необходимо их выносить в отдельный мультикуб. 

## Пример расчета с учетом CF0 (Первоначальных инвестиций)

В общем виде при расчете `NPV` первоначальные инвестиции не дисконтируются: 

![Формула для вычисления NPV - 001](f-npv-equation-001.png)

или: 

![Формула для вычисления NPV - 002](f-npv-equation-002.png)

Для корректного расчета значений `NPV` инвестиционного проекта нужно вынести первый денежный поток (`Initial Investment`) за функцию через отдельный мультикуб. 

Вынесите в отдельный мультикуб значения первого периода:  

![Вынесите в отдельный мультикуб значения первого периода](f-npv-source-001.png)

Перенесите денежные потоки на один поток влево: 

![Перенесите денежные потоки на один поток влево](f-npv-source-002.png)
 

Прибавьте первоначальные инвестиции к `NPV` от потоков, начиная с `CF1` – первого дисконтируемого денежного потока: 

![](f-npv-result-001.png)

!!! note "Важно"
    Без исключения первоначальных инвестиций из диапазона данных значение NPV искажается в большую или меньшую сторону, в зависимости от знака. 

 