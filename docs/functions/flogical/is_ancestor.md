---
uid: 'is_ancestor'
title: 'IS_ANCESTOR'
pdf: true
---
# IS_ANCESTOR
Возвращает `X` для элемента измерения в случае, если он является предком (элементом более высокого уровня) хотя бы для одного элемента из заданного множества.  

Функция является обратной функцией для [IS_DESCENDANT](is_descendant.md). 

## Синтаксис

IS_ANCESTOR(X, [Y1, Y2, ...]) 

## Аргументы 

- X: элемент измерения или выражение, возвращающее элемент измерения
- [Y1, Y2, ...]: множество элементов измерений или выражений, возвращающих элементы измерений. Функция возвращает `TRUE`, если `X` является предком для хотя бы одного элемента этого множества. 

## Возвращаемое значение 

логический формат / Boolean 

## Эквивалент в Excel

нет 

## Пример

Рассмотрим мультикуб в строках которого справочник `Материки и города`, а в колонках кубы. Чтобы выделить все элементы данного измерения более высокого уровня, чем `Каир` и `Сфакс`, используем формулу: 

```
IS_ANCESTOR(ITEM('Материки и города'), ['Материки и города'.'Сфакс', 'Материки и города'.'Каир'])
```

![Пример функции IS_ANCESTOR - выделить все элементы данного измерения более высокого уровня, чем `Каир` и `Сфакс`](f-is_ancestor-result-001.png)

Используем мультикуб `Селектор` с кубом `Город` для демонстрации работы функции:

Используем мультикуб `Селектор` с кубом `Город` для демонстрации работы функции:
![](f-is_ancestor-selector.png)

Тогда элементы измерения более высокого уровня, чем указанное значение города в дополнительном кубе `Город`, определим по формуле: 

```
IS_ANCESTOR(ITEM('Материки и города'), ['Селектор'.'Город'])
``` 

![Пример функции IS_ANCESTOR - определить элементы измерения более высокого уровня, чем указанное значение города в дополнительном кубе `Город`](f-is_ancestor-result-010.png)