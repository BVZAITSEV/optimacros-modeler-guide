---
uid: 'findbycode'
title: 'FINDBYCODE'
pdf: true
---
# FINDBYCODE
Принимает измерение `X` в качестве первого параметра и текст `Y` в качестве второго параметра. Возвращает элемент измерения с указанным кодом. 

Дополнительный параметр `Z1` уточняет, где будет происходить поиск: «0» – строго по указанному измерению, «1» – по всей иерархии указанного измерения. Если параметр не указан, по умолчанию поиск элемента происходит строго по указанному измерению `X`. 

Дополнительный параметр `Z2` уточняет чувствительность к регистру: «0» – учитывать регистр, «1» – не учитывать регистр. Если параметр не указан, по умолчанию поиск элемента происходит с учетом регистра. 

К аргументу `Y` функции `FINDBYCODE` применяется функция [CLEAN](clean.md). Чтобы выполнять поиск точного вхождения `Y`, используйте функцию [FINDBYCODE_EXACT](findbycode.md). 

## Синтаксис

FINDBYCODE(X, Y) 

FINDBYCODE(X, Y, Z)  

FINDBYCODE_EXACT(X, Y) 

FINDBYCODE_EXACT(X, Y, Z) 

## Аргументы 

- X: формат справочника или его выборка
- Y: текстовое выражение, возвращающее код искомого элемента
- Z1: дополнительный параметр, уточняющий место поиска элемента: «0» или "list" – строго по указанному измерению, «1» или "hierarchy"  – по всей иерархии указанного измерения
- Z2: дополнительный параметр, определяющий чувствительность к регистру: «0» или «case_sensitive» – учитывать регистр, «1» или «case_insensitive» – не учитывать регистр 

## Возвращаемое значение 

Элемент измерения `X`.

## Эквивалент в Excel

ВПР/LOOKUP 

## Пример

В данном примере функция `FINDBYCODE` возвращает элементы справочника `Контакты` по указанным кодам. 

![Справочник Контакты](f-findbyname-img-contacts.png)
 


![Пример функций FINDBYCODE и FINDBYCODE_EXACT](f-findbycode-result.png)

```
FINDBYCODE(Контакты, Код)
```

выполняет поиск точного вхождения
```
FINDBYCODE_EXACT(Контакты, Код) 
```

